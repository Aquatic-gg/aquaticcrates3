name: AquaticCrates3
version: '${project.version}'
main: gg.aquatic.aquaticcrates.plugin.CratesPlugin
api-version: 1.21
author: LARKYY
folia-supported: true

dependencies:
  server:
    Waves:
      load: BEFORE
      required: true
    PlaceholderAPI:
      load: BEFORE
      required: false
    Nexo:
      load: BEFORE
      required: false