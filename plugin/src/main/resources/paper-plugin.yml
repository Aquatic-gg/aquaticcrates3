name: AquaticCrates3
version: '${project.version}'
main: gg.aquatic.aquaticcrates.plugin.CratesPlugin
api-version: 1.21
author: LARKYY
folia-supported: true

dependencies:
  server:
    Waves:
      load: BEFORE
      required: false
    Comet:
      load: BEFORE
      required: false
    PlaceholderAPI:
      load: BEFORE
      required: false
    Nexo:
      load: BEFORE
      required: false
    CraftEngine:
      load: BEFORE
      required: false
    HMCCosmetics:
      load: BEFORE
      required: false
    Eco:
      load: AFTER
      required: false
    EcoItems:
      load: AFTER
      required: false